# E2E Console Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Chinese guided E2E test console for real StudyFlow AI LangChain4j + Milvus RAG testing.

**Architecture:** Add independent static resources under `backend/src/main/resources/static` so the existing `index.html` console remains untouched. Extend static resource tests and update README usage instructions without committing local secrets.

**Tech Stack:** Spring Boot static resources, vanilla HTML/CSS/JavaScript, JUnit 5, MockMvc.

---

## Task 1: Static Resource Tests

**Files:**

- Modify: `backend/src/test/java/com/studyflow/ai/StaticConsoleResourceTests.java`

- [ ] Add tests for `GET /e2e.html`, `GET /e2e.js`, and `GET /e2e.css`.
- [ ] Run `.\mvnw.cmd -Dtest=StaticConsoleResourceTests test` and verify the tests fail because the resources do not exist.

## Task 2: E2E Page

**Files:**

- Create: `backend/src/main/resources/static/e2e.html`
- Create: `backend/src/main/resources/static/e2e.css`
- Create: `backend/src/main/resources/static/e2e.js`

- [ ] Implement a Chinese guided workflow page with health, login/register, upload, parse, summary, QA, and request log sections.
- [ ] Persist JWT token to `localStorage`.
- [ ] Auto-fill latest `materialId` and `sessionId`.
- [ ] Use fetch with `Authorization: Bearer <token>` for protected APIs.
- [ ] Run `.\mvnw.cmd -Dtest=StaticConsoleResourceTests test` and verify it passes.

## Task 3: README And Verification

**Files:**

- Modify: `backend/README.md`

- [ ] Add `/e2e.html` to common endpoints and recommended E2E flow.
- [ ] Explain that real AI/RAG requires local profile with LangChain4j provider, real API key, and Milvus running.
- [ ] Run `.\mvnw.cmd test`.
- [ ] Commit and push through `codex/bootstrap`.

## Self-Review

- Spec coverage: E2E page, LangChain4j activation guidance, tests, and README are covered.
- Secret safety: no API keys are committed.
- Scope control: this is a static test console, not a production Vue frontend.
